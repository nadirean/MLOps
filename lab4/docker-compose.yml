version: '3.9'

services:
  timescaledb:
    image: timescale/timescaledb-ha:pg16
    container_name: vectorscaledb
    restart: unless-stopped
    environment:
      POSTGRES_PASSWORD: password
      POSTGRES_USER: postgres
    ports:
      - "5432:5432"
    volumes:
      # Mount the SQL init script into the container init directory
      - ./initdb/create_similar_search_service_db.sql:/docker-entrypoint-initdb.d/prepare_similarity_search_service_db.sql:ro
      # Persist database data
      - timescale_data:/var/lib/postgresql/data

volumes:
  timescale_data:
    driver: local